
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Your Next.js configuration options can go here.
  // The 'srcDir' property is not needed as Next.js automatically
  // detects the 'src' directory.
};

export default nextConfig;
